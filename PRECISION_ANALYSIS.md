# 白球对准鬼球后进球不稳定：精度分析与修复报告

日期：2026-08-31

## 结论摘要

“白球中心严格放到鬼球中心，但有时进、有时不进”不能由当前合成基准的 0.5--0.7 px 径向误差解释。合成基准只说明理想图像中的检测器误差较小，不能证明真实截图中球心、台面映射和游戏碰撞模型同样准确。真实桌面上，2--5 px 的白球或目标球中心误差，在薄切球和远距离路线中足以造成明显的入射偏差。

本次确认的几何结论如下：

1. 鬼球中心必须位于目标球沿“目标球到袋口”方向的反向，距离为 `r_cue + r_target`。等半径时就是 `2r`，不是额外再加一个 `2r`。
2. `contact_pos()` 返回目标球背向袋口一侧的球面接触点，用于显示和几何说明；白球瞄准仍应对准鬼球中心。将切球厚度再次加到鬼球位置会重复计算。
3. 当前 `main.py` 的单应矩阵方向是正确的：`H = screen -> table`，`Hinv = table -> screen`。历史上曾有反向存储问题，但当前版本已修复；不能把该历史问题直接当成当前根因。
4. 当前 `aim_offset_x/y` 和 `pocket_offsets` 已经会进入规划，但没有足够的可视诊断，也没有根据真实进球反馈自动标定。
5. 本次修复没有擅自放宽或收紧入射角阈值；增加了统一的入射余弦计算，并修复了袋口精修/偏移后可能绕过入射角检查的问题。

## 代码路径核查

### `aimtool/physics.py`

`ghost_pos()` 使用：

```text
ghost = target - normalize(pocket - target) * (cue_radius + target_radius)
       + aim_offset
```

这正是两个刚性圆球接触时的球心距离。旧 API 只传一个 `r` 时退化为 `target - 2r*d`，测试已经覆盖。`impact_ghost()` 对开局解球使用同样的球心间距，但方向由母球指向目标球决定。

`contact_pos()` 使用目标球半径，把点放在 `target - target_radius*d`。它是目标球表面接触点，不是白球中心位置，也不是需要再次加入半径的瞄准点。

`direct_shot()` 和 `kick_shot()` 都把 `ghost_offset` 传给鬼球计算。`plan_shots()` 过滤障碍路线，再调用 `pocket_entry_ok()`。因此 `aim_offset` 会影响直球和库边解围的鬼球，不只是绘制。

原有 `pocket_entry_ok()` 的阈值为：中袋 `cos >= 0.55`，角袋 `cos >= 0.42`。这相当于中袋约 57 度、角袋约 65 度的保守入口限制。原实现按 `p[1] <= r` 或 `p[1] >= h-r` 判断长边袋口；袋口经过真实截图精修或用户偏移后，中心可能落在该范围之外，从而不再检查入射角。这是本次修复的明确逻辑缺陷。

### `aimtool/vision.py`

`homography()` 由检测到的屏幕四边形映射到标准台面坐标，`point_screen_to_table()` 使用 H，反向绘制使用 Hinv。台面锁定器减少了每帧 `find_table()` 抖动，但仍不能消除首帧四边形误差、窗口移动、缩放和边缘检测误差。

球检测优先使用颜色/连通域，满足条件时通过圆周边缘做亚像素圆拟合；拟合只对球心和局部半径提供改进，不会创造超过原始截图分辨率的信息。`estimate_ball_radius()` 取非红球亚像素半径的中位数，并用 MAD 排除异常值，最后限制在配置半径的 0.80--1.25 倍范围内。该方法能校正全局缩放，但不是透视相机下的局部尺度场；如果 H 有残差或某区域被非均匀拉伸，单一全局半径仍会留下系统误差。

`pick_cue()` 在多个白球候选中选半径最接近标准半径者。它没有使用历史轨迹、位置先验或局部圆拟合残差作二次选择，因此白色 UI、反光或重复候选仍可能造成偶发换球。

### `aimtool/config.py`

当前相关默认值为：

| 参数 | 当前默认 | 作用 |
|---|---:|---|
| `ball_radius_ratio` | `0.01125` | 标准台面宽度下的初始球半径 |
| `ball_radius_scale` | `1.0` | 全局半径标定倍率 |
| `ball_radius_instance_weight` | `0.35` | 亚像素单球半径参与碰撞半径的权重 |
| `aim_offset_x/y` | `0/0` | 鬼球在标准台面坐标中的固定偏移 |
| `pocket_offsets` | 6 个 `[0, 0]` | 每个袋口的局部偏移 |
| `rail_inset_ratio` | `1.0` | 库边按球心轨迹内缩的球半径倍率 |
| `power_gain/bias` | `1.0/0.0` | 推荐力度的倍率和偏置 |
| `pocket_accept_ratio` | `1.45` | 路线评分的袋口接受半径倍率 |

这些字段会通过 JSON 持久化。它们解决了“参数不能调”的问题，但目前没有真实进球/失球数据闭环，因此默认的 `aim_offset=0` 只是理想几何假设，不是实机标定结果。

### `main.py`

`_stage_balls()` 将分析图球心和半径换算到标准台面坐标，估计全局半径并应用 `ball_radius_scale`。随后 `_stage_targets()` 使用每球半径、`aim_offset_x/y` 生成 `ghost_offset`，`_stage_plan()` 将其传入直球和库边规划，并用 `rail_inset_ratio` 计算库边内缩距离。

袋口偏移是在球检测阶段应用到 `pockets_t`，所以会影响后续路线规划和绘制。力度增益只影响推荐力度，不会改变鬼球几何；不能把力度不合适误认为球心定位误差，但速度、旋转和库边能量损失仍可能造成游戏内实际偏差。

## 误差来源排序

### 1. 真实球心视觉误差

`bench_results.json` 的代表性结果为 45/45 检测，径向平均约 0.494 px，P50 约 0.511 px，P95 约 0.764 px。这是合成/标注基准结果，且匹配距离为 33.75 标准台面坐标单位；不能推导出真实游戏画面的 2--5 px 误差不存在。

真实误差常见来源包括球高光、阴影、红球粘连、白色 UI、压缩和截图时序。白球中心若有 3 px 偏差，显示层仍可能看起来“对准”，但实际球心连线已偏离。

### 2. 半径误差

鬼球沿目标球到袋口方向移动 `r_cue+r_target`。因此半径误差会直接变成鬼球沿该方向的位移。当前全局中位数和实例半径混合已经比固定半径稳健，但真实皮肤的可见边缘不一定等于碰撞圆边缘，`ball_radius_scale` 仍需实测标定。

### 3. 单应矩阵与四边形抖动

H 的方向当前正确，但正确方向不代表四个角点是真值。四边形角点各自只有 2--3 px 的误差时，台面中心附近的映射误差可能较小，靠近边缘和袋口时会增大；非整数 DPI 缩放还会让屏幕坐标与窗口坐标出现固定比例误差。`TableTracker` 只能抑制帧间跳动，不能修正稳定但错误的台面边界。

### 4. 未标定的鬼球系统偏移

默认 `aim_offset_x/y=0`。如果实机始终向同一方向偏，应该优先用该参数修正，而不是修改检测球心。建议每个方向先以 1--3 个标准台面坐标单位为步长试打，并记录同类球、相近距离、相近力度的结果。

### 5. 袋口入口模型

当前中袋/角袋余弦限制是经验阈值，不是 QQ 游戏碰撞源码。过严会少给路线，过松会推荐撞袋角的路线。袋口实际洞口中心还受到 `refine_pockets()`、内缩偏移和库边宽度影响。本次新增诊断值后，可以直接观察 `entry_cos` 与 `entry_cos_min`，再判断是模型过滤还是实际击球失败。

### 6. 游戏物理差异

当前模型把球视为二维刚性圆，库边使用固定能量损失，未显式建模摩擦、旋转、击球点偏心、球杆偏移、碰撞恢复系数和 QQ 的特殊容错。特别是薄切、贴库和低力度路线，即使几何中心准确，也可能产生可见差异。

### 7. 接触点/切球厚度误解

白球中心到达鬼球中心时，两个球心距离已经是两个半径之和。`contact_pos()` 只用于表示目标球表面的接触位置。不能把 `contact_pos()` 再当作白球瞄准中心，也不能在鬼球距离上额外加半径。

## 本次代码改动

### 物理模块

新增 `pocket_entry_cos()` 和 `pocket_entry_limit()`，统一计算入口方向余弦和阈值。`pocket_entry_ok()` 现在对袋口内缩/偏移使用球半径容差识别长边袋口，避免精修袋口后意外绕过入口角过滤；原有中袋、角袋阈值保持不变。

### 主流程诊断

最终 `scene["aim_geometry"]` 增加：

- `ball_radius`：当前帧用于规划的半径；
- `ball_radius_reference`、`ball_radius_error`、`ball_radius_error_pct`：相对配置基准的偏差；
- `entry_cos`、`entry_cos_min`：当前路线的入口余弦和最低要求；
- `aim_offset`：本帧实际使用的鬼球偏移。

状态栏也显示当前半径及入口余弦，便于真实打球时截图或记录。这里的 `ball_radius_error` 是相对配置基准的诊断，不是假装知道真实球心真值。

### 回归测试

新增测试覆盖“袋口中心向台内内缩后仍然执行中袋入射角过滤”。验证结果：

```text
tests/test_pocket_entry.py: 8 passed
全量 pytest: 157 passed in 17.89s
```

本轮曾出现过一次新增测试构造错误，原因是测试把母球坐标元组误传给 `pocket_entry_cos()`；修正测试后，专项测试、四个核心模块的 `py_compile` 和完整测试套件均已通过。

## 建议的下一步标定

1. 增加真实反馈记录：每次显示方案时保存时间、球/袋口、`aim_offset`、半径、`entry_cos`、切角、力度和用户反馈（进/不进）。
2. 只在同一目标球、相近距离和相近力度的样本达到最小数量后更新偏移，使用带衰减的平均或小步长更新，避免把偶发失误当成系统偏差。
3. 将全局 `aim_offset_x/y` 与每袋 `pocket_offsets[i]` 分开拟合：全局偏移解释球心/杆线系统误差，袋口偏移解释袋洞定位误差。
4. 将标定限制在合理范围，并保留撤销/备份；不要让一次错误反馈把鬼球移到一个球半径之外。
5. 增加 Alt+方向键微调时，先只修改内存中的偏移，松键或显式确认后保存 JSON；每步建议 1--2 个标准台面坐标单位，并在 UI 中显示当前偏移。
6. 对 `find_table()` 的四角做重投影残差和帧间位移统计；如果残差或位移超限，隐藏路线并提示重新框选，而不是继续使用看似稳定但错误的 H。
7. 对白球候选加入跨帧轨迹和圆拟合残差评分，降低白色 UI/反光被选为母球的概率。

## 当前边界

本次改动修复了一个明确的入口角过滤边界问题，并增加了定位半径和入射角可观测性；它不能单独证明真实游戏已经达到稳定进球。要把“有时进、有时不进”进一步收敛到数值，需要真实截图/击球结果样本，至少覆盖直球、薄切、中袋、角袋、贴库和不同窗口 DPI。
